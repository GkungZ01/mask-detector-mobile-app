import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.gkungz01.maskdetector",
  appName: "Mask Detector",
  webDir: "dist",
};

export default config;
